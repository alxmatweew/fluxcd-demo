apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: local-path-provisioner-repo
  namespace: flux-system
spec:
  interval: 5m
  url: https://openchart.choerodon.com.cn/choerodon/c7n  # URL вашего Helm-репозитория
#  secretRef:  # Опционально, если требуется аутентификация
#    name: helm-repo-credentials